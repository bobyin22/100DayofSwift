//: [Previous](@previous)

import Foundation
import UIKit

//Swift, implicitly unwrapped optionals (IUOs)
//@IBOutlet weak var myBtn: UILabel!      //這是Swift早期因為UIKit才有的寫法，知道一定會有按鈕，但是現在給nil


/*
 UIKit has a performance optimization that means the image is only created when that piece of user interface is actually shown. Apple pushes back the work of creating the image until it’s actually needed, like a lazy Swift property,
 */
//: [Next](@next)
