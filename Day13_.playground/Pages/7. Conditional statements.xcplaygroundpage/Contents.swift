import UIKit

// 如果 A是true && B是true 進入body
var action: String
var stayOutTooLate = false //true
var nothingInBrain = false //true

if !stayOutTooLate && !nothingInBrain {
    action = "cruise"
    print("123")
}
